#include "stdafx.h";

void main(void)
{
    char A = '1';
    count << sizeof(A) << endl;
    int C = A > B ? 1 : 0;
    count << A<B? 1: 0 << endl;
    count << D << endl;
    int E = (A, B, C);
    count << E << endl;
    float F = float(E);
    count << F << endl;
    count << &F << endl;
    float *p = &F;
    count << P << endl;
    count << *P << endl;
}
